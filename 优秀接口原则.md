优秀的接口设计，本质上是**调用者与实现者之间的一份清晰、稳定、可演进的契约**。一句话概括：**让调用者容易做对、难做错，让实现者能隐藏复杂度并持续演进。**

无论是 HTTP API、RPC、库函数还是模块边界，优秀接口通常具备这些特征：

### 1. 职责单一，边界清晰
一个接口只解决一类问题，不把无关能力塞在一起。  
避免“万能接口”：参数一大堆、行为靠 flag 切换、返回结构随场景变化。

### 2. 命名一致，语义明确
命名要自解释，术语统一。  
HTTP API 中资源用名词复数：`/users/{id}/orders`；方法用 `GET/POST/PUT/PATCH/DELETE` 表达语义。  
函数名要表达意图，避免 `doData()`、`handle()`、`process()` 这类模糊命名。

### 3. 最小暴露，隐藏实现
只暴露必要的信息和操作，不泄露内部数据结构、数据库字段、实现细节。  
接口越窄，耦合越低，越容易替换实现。

### 4. 契约稳定，兼容演进
不轻易做破坏性变更。原则是：**只增不改不删**。  
新增字段可选、枚举可扩展、默认值合理；废弃要有过渡期和文档说明。  
HTTP API 常用 `/v1` 或 Header 做版本控制。

### 5. 防错设计，错误可处理
参数要有类型约束、范围校验和明确默认值。  
错误响应要结构化、可机器处理，例如：

```json
{
  "code": "ORDER_NOT_FOUND",
  "message": "订单不存在",
  "details": { "order_id": "123" },
  "trace_id": "abc-123"
}
```

状态码要准确：`400` 参数错误、`401` 未认证、`403` 无权限、`404` 不存在、`409` 冲突、`422` 校验失败、`429` 限流、`500` 服务端错误。

### 6. 幂等、安全、可观测
创建类接口支持幂等键，避免重复下单、重复支付。  
认证、授权、限流、审计、输入校验缺一不可。  
关键请求要有 `trace_id`，日志、指标、链路追踪能串起来。

### 7. 分页、过滤、排序要统一
列表接口不能一次返回全部数据。  
统一使用 `limit/cursor` 或 `page/size`，过滤排序用 query 参数：

```http
GET /v1/orders?status=paid&limit=20&cursor=xxx
```

### 8. 文档与工具友好
提供 OpenAPI/Swagger、示例、SDK、变更日志。  
好的接口应该让调用者看文档就能写对，而不是靠读源码或反复试错。

### 9. 函数/模块接口的额外要点
参数少，避免布尔参数；输入输出类型明确；失败用异常或 `Result`，不要返回 `null` 魔法值；无副作用或显式声明副作用；依赖注入，便于测试和替换。

### 10. 性能与粒度平衡
接口太细会导致 N+1 调用，太粗会变成万能接口。  
合适粒度是：一次调用能完成一个完整业务动作，同时不过度传输数据。

### 反模式
- 命名混乱、风格不一
- 返回 `null` 或魔法数字
- 错误码随意、无上下文
- 破坏性变更无版本、无弃用
- 泄露数据库结构
- 万能接口、布尔参数爆炸
- 无分页、无幂等、无限流

**检查清单：**
调用者能否一眼看懂？  
一次能否写对？  
出错能否定位？  
升级后旧代码会不会崩？  
实现换了调用者是否无感？  
安全、幂等、可观测是否具备？

好接口让调用者“一眼懂、一次写对、出错能查、升级不崩”；坏接口让调用者猜、试、绕、骂。